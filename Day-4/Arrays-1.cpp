// Problem: Write a program to calculate the sum of all elements in an integer array.
// Input: The user will input the size of the array and then the elements of the array.
// Output: The sum of all elements.

#include <iostream>
using namespace std;
int main()
{
    // srand(time(0));
    double random=rand();
    cout<<random<<endl;
    double random2=rand();
    cout<<random2;
    // int num = 1050 % 50;
    // cout<<num<<endl;
    // int n;
    // cin >> n;

    // int arr[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> arr[i];
    // }
    // int sum = 0;
    // for (int i = 0; i < n; i++)
    // {
    //     sum = sum + arr[i];
    // }
    // cout << sum;
    return 0;
}